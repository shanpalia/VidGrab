import fs from 'node:fs';import path from 'node:path';
const f=path.join(process.cwd(),'android','app','src','main','java','com','shanpalia','vidgrab','MainActivity.java');if(!fs.existsSync(f))throw new Error('MainActivity.java not found');let t=fs.readFileSync(f,'utf8');if(!t.includes('import android.graphics.Color;'))t=t.replace('import android.content.ContentResolver;\n','import android.content.ContentResolver;\nimport android.graphics.Color;\n');if(!t.includes('import android.view.View;'))t=t.replace('import android.webkit.JavascriptInterface;\n','import android.webkit.JavascriptInterface;\nimport android.view.View;\n');const s=t.indexOf('    @Override\n    public void onCreate(Bundle savedInstanceState) {'),e=t.indexOf('\n    }\n}\n\nclass VidGrabNative',s);if(s<0||e<0)throw new Error('onCreate not found');const m=`    @Override
    public void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        if(Build.VERSION.SDK_INT>=Build.VERSION_CODES.R)getWindow().setDecorFitsSystemWindows(false);
        getWindow().setStatusBarColor(Color.WHITE);getWindow().setNavigationBarColor(Color.WHITE);
        if(Build.VERSION.SDK_INT>=Build.VERSION_CODES.M){int f=View.SYSTEM_UI_FLAG_LIGHT_STATUS_BAR;if(Build.VERSION.SDK_INT>=Build.VERSION_CODES.O)f|=View.SYSTEM_UI_FLAG_LIGHT_NAVIGATION_BAR;getWindow().getDecorView().setSystemUiVisibility(f);}
        View decor=getWindow().getDecorView();decor.setOnApplyWindowInsetsListener((v,insets)->{int top=0,bottom=0;if(Build.VERSION.SDK_INT>=Build.VERSION_CODES.R){android.graphics.Insets b=insets.getInsets(android.view.WindowInsets.Type.systemBars()|android.view.WindowInsets.Type.displayCutout());top=b.top;bottom=b.bottom;}else if(Build.VERSION.SDK_INT>=Build.VERSION_CODES.P&&insets.getDisplayCutout()!=null){top=insets.getDisplayCutout().getSafeInsetTop();bottom=insets.getDisplayCutout().getSafeInsetBottom();}if(bridge!=null&&bridge.getWebView()!=null){View w=bridge.getWebView();w.setPadding(w.getPaddingLeft(),top,w.getPaddingRight(),bottom);}return insets;});
        if(bridge!=null&&bridge.getWebView()!=null){bridge.getWebView().getSettings().setJavaScriptEnabled(true);bridge.getWebView().getSettings().setDomStorageEnabled(true);bridge.getWebView().addJavascriptInterface(new VidGrabNative(this),"VidGrabNative");}
        decor.post(()->decor.requestApplyInsets());
    }`;t=t.slice(0,s)+m+t.slice(e+6);fs.writeFileSync(f,t);console.log('camera safe-area patch complete');
