{
  "name": "vidgrab",
  "private": true,
  "version": "1.0.1",
  "type": "module",
  "scripts": {
    "dev": "tsx server.ts",
    "build": "node scripts/verify-build-source.mjs && node scripts/fix-app-layout.mjs && node scripts/final-search.mjs && node scripts/final-ui.mjs && node scripts/final-grab.mjs && node scripts/final-grab-popup.mjs && vite build && node scripts/verify-build-output.mjs && esbuild server.ts --bundle --platform=node --format=cjs --packages=external --sourcemap --outfile=dist/server.cjs",
    "start": "node dist/server.cjs",
    "preview": "vite preview",
    "clean": "rm -rf dist server.js android",
    "lint": "tsc --noEmit",
    "android:prepare": "npm run build && rm -f dist/server.cjs dist/server.cjs.map && rm -rf android && npx cap add android && npx cap copy android && node scripts/patch-android.mjs && node scripts/native-browser.mjs && node scripts/fix-native-browser-source.mjs && node scripts/native-browser-grab.mjs && node scripts/final-vidmate-browser.mjs && node scripts/final-native.mjs && node scripts/fix-edge-to-edge.mjs",
    "android:build": "npm run android:prepare && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@google/genai": "^2.4.0",
    "@tailwindcss/vite": "^4.1.14",
    "@vitejs/plugin-react": "^5.0.4",
    "lucide-react": "^0.546.0",
    "react": "^19.0.1",
    "react-dom": "^19.0.1",
    "vite": "^6.2.3",
    "express": "^4.21.2",
    "dotenv": "^17.2.3",
    "motion": "^12.23.24",
    "@capacitor/core": "^7.4.0",
    "@capacitor/android": "^7.4.0"
  },
  "devDependencies": {
    "@types/node": "^22.14.0",
    "@types/express": "^4.17.21",
    "autoprefixer": "^10.4.21",
    "esbuild": "^0.25.0",
    "tsx": "^4.21.0",
    "@capacitor/cli": "^7.4.0"
  }
}
